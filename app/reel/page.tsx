import ReelPageClient from "@/components/ReelPageClient";

export const metadata = {
  title: "Reel — Brian Wade Scott",
  description: "Motion reel — Brian Wade Scott, Creative Director.",
};

export default function ReelPage() {
  return <ReelPageClient />;
}
